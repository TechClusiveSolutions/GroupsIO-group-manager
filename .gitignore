# Composer
/vendor/

# wp-env
/.wp-env.override.json

# Env files are not this project's credential mechanism (see
# wp-config.php constants / .wp-env.override.json above) but are
# excluded defensively in case one is ever created locally.
.env
.env.*

# PHPUnit / coverage
/coverage/
.phpunit.result.cache

# Node (tooling only, no runtime JS dependency for this plugin)
/node_modules/

# OS/editor cruft
.DS_Store
Thumbs.db

# Playwright E2E
/tests/e2e/.auth/
/playwright-report/
/test-results/
